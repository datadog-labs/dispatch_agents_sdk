# Lessons

- When a review comment questions a reusable workflow input, verify which input is actually confusing before simplifying the interface. Preserve clearly useful configuration inputs and remove the ambiguous one instead of guessing.
