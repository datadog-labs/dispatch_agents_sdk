[project]
name = "company-researcher"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "claude-agent-sdk>=0.1.29",
    "dispatch-agents",
]

[tool.uv.sources]
dispatch-agents = { path = "../../sdk" }
