namespace: examples
agent_name: hello-world
entrypoint: agent.py
system_packages: []
volumes:
- name: data
  mountPath: /data
  mode: read_write_many
resources:
  limits:
    cpu: 250m
    memory: 2Gi
network:
  egress:
    allow_domains:
    - match_name: jsonplaceholder.typicode.com
