[project]
name = "hello-world"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "dispatch-agents",
    "pytest>=8.0.2",
    "pytest-asyncio>=0.23.8",
]

[tool.uv.sources]
dispatch-agents = { path = "../../sdk" }
