[project]
name = "daily-digest"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "dispatch-agents",
    "httpx>=0.27.0",
]

[tool.uv.sources]
dispatch-agents = { path = "../../sdk" }
