[project]
name = "knowledge-base-query"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "claude-agent-sdk>=0.1.29",
    "dispatch-agents",
    "httpx",
]

[tool.uv.sources]
dispatch-agents = { path = "../../sdk" }
